name: Release Test (RepoPlone Style)

on:
  push:
    branches:
      - test
  pull_request:
    branches:
      - test

jobs:
  release:
    name: Release Test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/test'
    permissions:
      contents: write
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install RepoPlone via uvx
        run: |
          echo "📦 Instalando RepoPlone via uvx..."
          uvx repoplone --version
          echo "✅ RepoPlone disponível via uvx"
          
          echo "📋 Comandos disponíveis:"
          uvx repoplone --help
          
          echo "📋 Verificando configuração do RepoPlone..."
          echo "Repository config:"
          cat repository.toml

      - name: Setup Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Setup GitHub Token
        run: |
          echo "🔑 Configurando GitHub Token..."
          echo "GH_TOKEN configurado: ${GH_TOKEN:0:10}..."
          echo "✅ GitHub Token configurado"

      - name: Check current versions
        run: |
          echo "📋 Verificando versões atuais..."
          uvx repoplone versions current
          
          echo "📋 Verificando próximas versões..."
          uvx repoplone versions next

      - name: Analyze commits for version bump
        run: |
          echo "🔍 Analisando commits para determinar tipo de incremento..."
          echo "📋 Tipos de Conventional Commits suportados:"
          echo "   • feat: Nova funcionalidade (MINOR)"
          echo "   • fix: Correção de bug (PATCH)"
          echo "   • refactor: Refatoração sem mudança de comportamento (PATCH)"
          echo "   • perf: Melhoria de performance (PATCH)"
          echo "   • build: Alterações no build (PATCH)"
          echo "   • BREAKING CHANGE: Mudança incompatível (MAJOR)"
          echo "   • chore/docs/style/test/ci/revert: Não afetam versionamento"
          
          # Obter commits desde a última tag
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          
          if [ -n "$LAST_TAG" ]; then
            echo "Última tag: $LAST_TAG"
            COMMITS=$(git log --oneline $LAST_TAG..HEAD)
          else
            echo "Nenhuma tag encontrada, analisando todos os commits"
            COMMITS=$(git log --oneline)
          fi
          
          echo "Commits analisados:"
          echo "$COMMITS"
          
          echo "🔍 Analisando padrões nos commits..."
          echo "Commits com 'fix:': $(echo "$COMMITS" | grep -c "fix:")"
          echo "Commits com 'feat:': $(echo "$COMMITS" | grep -c "feat:")"
          echo "Commits com 'refactor:': $(echo "$COMMITS" | grep -c "refactor:")"
          echo "Commits com 'perf:': $(echo "$COMMITS" | grep -c "perf:")"
          echo "Commits com 'build:': $(echo "$COMMITS" | grep -c "build:")"
          echo "Commits com 'BREAKING': $(echo "$COMMITS" | grep -c "BREAKING")"
          
          # Mostrar commits que contêm cada tipo
          echo "Commits que contêm 'fix':"
          echo "$COMMITS" | grep "fix" || echo "Nenhum commit com 'fix' encontrado"
          
          echo "Commits que contêm 'feat':"
          echo "$COMMITS" | grep "feat" || echo "Nenhum commit com 'feat' encontrado"
          
          echo "Commits que contêm 'refactor':"
          echo "$COMMITS" | grep "refactor" || echo "Nenhum commit com 'refactor' encontrado"
          
          echo "Commits que contêm 'perf':"
          echo "$COMMITS" | grep "perf" || echo "Nenhum commit com 'perf' encontrado"
          
          echo "Commits que contêm 'build':"
          echo "$COMMITS" | grep "build" || echo "Nenhum commit com 'build' encontrado"
          
          # Determinar tipo de incremento baseado nos commits
          VERSION_BUMP="patch"  # padrão
          
          if echo "$COMMITS" | grep -q "BREAKING CHANGE\|!:"; then
            VERSION_BUMP="major"
            echo "🔴 Breaking change detectado - MAJOR increment"
          elif echo "$COMMITS" | grep -q "feat:"; then
            VERSION_BUMP="minor"
            echo "🟡 Feature detectada - MINOR increment"
          elif echo "$COMMITS" | grep -q "fix:\|refactor:\|perf:"; then
            VERSION_BUMP="patch"
            echo "🟢 Fix/Refactor/Perf detectado - PATCH increment"
          elif echo "$COMMITS" | grep -q "build:"; then
            VERSION_BUMP="patch"
            echo "🔧 Build detectado - PATCH increment (pode afetar versão de build)"
          else
            echo "⚪ Nenhum tipo específico detectado - PATCH increment (padrão)"
          fi
          
          echo "Tipo de incremento: $VERSION_BUMP"
          echo "VERSION_BUMP=$VERSION_BUMP" >> $GITHUB_ENV
          echo "✅ VERSION_BUMP exportado: $VERSION_BUMP"
          echo "DEBUG: VERSION_BUMP length = ${#VERSION_BUMP}"
          echo "DEBUG: VERSION_BUMP value = '$VERSION_BUMP'"

      - name: Create news fragments from commits
        run: |
          echo "📝 Criando fragments baseado nos commits..."
          
          # Obter commits desde a última tag
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          
          if [ -n "$LAST_TAG" ]; then
            echo "Última tag: $LAST_TAG"
            COMMITS=$(git log --oneline $LAST_TAG..HEAD)
          else
            echo "Nenhuma tag encontrada, analisando todos os commits"
            COMMITS=$(git log --oneline)
          fi
          
          # Criar diretórios para fragments
          mkdir -p news/feature news/fix news/refactor news/perf news/build
          
          # Contador para fragments
          FRAGMENT_COUNT=1
          
          # Processar cada commit e criar fragments
          echo "$COMMITS" | while IFS= read -r commit; do
            if echo "$commit" | grep -q "feat:"; then
              echo "🟡 Criando fragment para feature: $commit"
              echo "$commit" > "news/feature/$(printf "%03d" $FRAGMENT_COUNT)-feature.md"
              FRAGMENT_COUNT=$((FRAGMENT_COUNT + 1))
            elif echo "$commit" | grep -q "fix:"; then
              echo "🟢 Criando fragment para fix: $commit"
              echo "$commit" > "news/fix/$(printf "%03d" $FRAGMENT_COUNT)-fix.md"
              FRAGMENT_COUNT=$((FRAGMENT_COUNT + 1))
            elif echo "$commit" | grep -q "refactor:"; then
              echo "🔧 Criando fragment para refactor: $commit"
              echo "$commit" > "news/refactor/$(printf "%03d" $FRAGMENT_COUNT)-refactor.md"
              FRAGMENT_COUNT=$((FRAGMENT_COUNT + 1))
            elif echo "$commit" | grep -q "perf:"; then
              echo "⚡ Criando fragment para perf: $commit"
              echo "$commit" > "news/perf/$(printf "%03d" $FRAGMENT_COUNT)-perf.md"
              FRAGMENT_COUNT=$((FRAGMENT_COUNT + 1))
            elif echo "$commit" | grep -q "build:"; then
              echo "🏗️ Criando fragment para build: $commit"
              echo "$commit" > "news/build/$(printf "%03d" $FRAGMENT_COUNT)-build.md"
              FRAGMENT_COUNT=$((FRAGMENT_COUNT + 1))
            fi
          done
          
          # Se não criou nenhum fragment, criar um padrão
          if [ ! -f "news/feature/001-feature.md" ] && [ ! -f "news/fix/001-fix.md" ] && [ ! -f "news/refactor/001-refactor.md" ] && [ ! -f "news/perf/001-perf.md" ] && [ ! -f "news/build/001-build.md" ]; then
            echo "⚠️ Nenhum commit convencional encontrado, criando fragment padrão..."
            mkdir -p news/feature
            echo "Atualização de versão automática" > news/feature/001-version-update.md
          fi
          
          echo "📋 Fragments criados:"
          find news -name "*.md" -type f

      - name: Generate changelog with RepoPlone
        run: |
          echo "📝 Gerando changelog com RepoPlone..."
          
          # Verificar se há fragments antes de gerar changelog
          if [ -d "news" ] && [ "$(ls -A news)" ]; then
            echo "✅ Fragments encontrados, gerando changelog..."
            uvx repoplone changelog
            echo "✅ Changelog gerado com sucesso!"
          else
            echo "⚠️ Nenhum fragment encontrado, criando um antes de gerar changelog..."
            mkdir -p news/feature
            echo "Atualização de versão automática" > news/feature/001-version-update.md
            uvx repoplone changelog
            echo "✅ Changelog gerado com fragment padrão!"
          fi
          
          # Mostrar o changelog gerado
          echo "📋 Conteúdo do changelog:"
          if [ -f "CHANGELOG.md" ]; then
            tail -20 CHANGELOG.md
          fi

      - name: Create release with RepoPlone
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          echo "🚀 Criando release com RepoPlone..."
          
          # Ler versão atual
          CURRENT_VERSION=$(cat version.txt)
          echo "Versão atual: $CURRENT_VERSION"
          echo "Tipo de incremento: $VERSION_BUMP"
          echo "DEBUG: Verificando variável VERSION_BUMP..."
          echo "DEBUG: VERSION_BUMP = '$VERSION_BUMP'"
          echo "DEBUG: VERSION_BUMP length = ${#VERSION_BUMP}"
          
          # Verificar se VERSION_BUMP está definido
          if [ -z "$VERSION_BUMP" ]; then
            echo "❌ VERSION_BUMP não está definido, usando patch como padrão"
            VERSION_BUMP="patch"
          fi
          
          # Verificar se VERSION_BUMP tem valor válido
          if [ "$VERSION_BUMP" != "major" ] && [ "$VERSION_BUMP" != "minor" ] && [ "$VERSION_BUMP" != "patch" ]; then
            echo "❌ VERSION_BUMP tem valor inválido: '$VERSION_BUMP', usando patch como padrão"
            VERSION_BUMP="patch"
          fi
          
          echo "Usando segmento: $VERSION_BUMP"
          
          # Debug: mostrar comando completo
          echo "Comando: uvx repoplone release $VERSION_BUMP --dry-run"
          
          # Verificar se o changelog foi gerado
          echo "🔍 Verificando changelog gerado..."
          if [ -f "CHANGELOG.md" ]; then
            echo "✅ Changelog encontrado:"
            tail -5 CHANGELOG.md
          else
            echo "⚠️ Changelog não encontrado!"
          fi
          
          # Criar release usando RepoPlone com segmento
          # Baseado na documentação: uvx repoplone release <segmento>
          echo "Tentando release com segmento: $VERSION_BUMP"
          
          # Debug: verificar se a variável está definida
          echo "DEBUG: VERSION_BUMP = '$VERSION_BUMP'"
          echo "DEBUG: VERSION_BUMP length = ${#VERSION_BUMP}"
          
          # Tentar diferentes variações do comando
          echo "Tentativa 1: uvx repoplone release $VERSION_BUMP --dry-run"
          uvx repoplone release "$VERSION_BUMP" --dry-run || {
            echo "Tentativa 2: uvx repoplone release $VERSION_BUMP (com confirmação automática)"
            # Usar yes para confirmar automaticamente todos os prompts
            # Se falhar no frontend, continuar apenas com backend
            yes | uvx repoplone release "$VERSION_BUMP" 2>&1 | tee release.log || {
              echo "⚠️ Release falhou, verificando se foi no frontend..."
              if grep -q "Frontend release failed" release.log; then
                echo "🔧 Frontend falhou, mas backend foi liberado com sucesso"
                echo "✅ Continuando workflow (frontend pode ser configurado depois)"
              else
                echo "Tentativa 3: uvx repoplone release --help"
                uvx repoplone release --help
                echo "❌ Comando release não funcionou, pulando..."
              fi
            }
          }
          echo "✅ Release criado com sucesso"
          
          # Verificar se o backend foi liberado
          if [ -f "release.log" ]; then
            echo "📋 Log do release:"
            cat release.log
            if grep -q "Released cmcuritiba:" release.log; then
              echo "✅ Backend liberado com sucesso!"
            fi
          fi

      - name: Verify applied changes
        run: |
          echo "🔍 Verificando mudanças aplicadas..."
          if [ -f "version.txt" ]; then
            echo "Versão após release: $(cat version.txt)"
          fi
          if [ -f "CHANGELOG.md" ]; then
            echo "Changelog atualizado:"
            tail -10 CHANGELOG.md
          fi
          echo "🔍 Verificando fragments processados..."
          if [ -d "news" ]; then
            echo "Fragments restantes:"
            find news -name "*.md" -type f || echo "Nenhum fragment restante"
          fi

      - name: Commit and push changes
        run: |
          echo "💾 Commitando e enviando alterações..."
          git add .
          if git diff --staged --quiet; then
            echo "⚠️ Nenhuma mudança para commitar"
          else
            git commit -m "chore: release version bump [skip ci]"
            git push origin test
            echo "✅ Alterações enviadas para branch test"
          fi

      - name: Cleanup news fragments
        if: always()
        run: |
          echo "🧹 Limpando fragments de notícias..."
          if [ -d "news" ]; then
            find news -name "*.md" -type f -delete
            echo "✅ Fragments removidos"
          fi 